#include <algorithm>
#include <array>
#include <iostream>
#include <iterator>
#include <vecotor>
#include <set>



int main() {
    std::set<int> st;
    std::map<char, int> mp;

    st.find(1) != st.end();
    st.find(1) != st.end();

    std::vecotor<int> vec;
    vec[1];

    std::cout << "\n";
    return 0;
}
